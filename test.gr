import Result from "result"

let variableDeclaration = () => {
  print("string test" ++ toString(770))
}

// this is a comment

let someThing = cool => {
  if (!cool) {
    Err("not cool")
  } else {
    Ok("nice")
  }
}
